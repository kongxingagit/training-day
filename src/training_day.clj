(ns training-day) ;declares a namespace ns called training day but hyphen instead of underscore.underscore for file nam

(def answer 42)

(def hai "O HAI!")

(defn square [x]
  (* x x ))

(defn average [a b]
  (/ (+ a b) 2))
